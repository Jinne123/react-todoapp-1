import "./Title.css";
import React from "react";

const Title = ({ todo, index, completeTodo, removeTodo }) => {
  return (
    <div className="Title">
      <h1>Pokemon battle</h1>
    </div>
  );
};

export default Title;
